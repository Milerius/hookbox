{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit:*)",
            "command": "cd /Users/milerius/Documents/hookbox && cargo fmt --all",
            "timeout": 30,
            "statusMessage": "Running cargo fmt --all..."
          }
        ]
      }
    ]
  }
}
