[package]
name = "hookbox"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Core library: traits, types, pipeline orchestration, and lightweight built-in implementations for a durable webhook inbox."

[dependencies]
